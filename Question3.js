// Create a function that takes voltage and current and returns the calculated power.
function calPower(voltage, current){
    //P = V * I
     return voltage * current;
 }
 
 console.log(calPower(3.5,140));